# 대규모업데이트 결과 (BIG-BATCH-1) — 2026-07-05

스펙: `대규모업데이트_spec.md` · 버전 v1.34 · 판정: **BIG_BATCH_1_PROGRESS** (스펙 기준 정상 — "A + 우선 몇 개 확실히 + 상태표")

## 완료 항목 (구현 + 검증)

### A · 시각 정리 — 4/4 완료 (스크린샷 확인)
| # | 항목 | 결과 |
|---|---|---|
| A1 | **벽 '표' 해소** | 쿠션벽을 수풀과 같은 **이웃 인식 오토타일**로 — 붙은 셀은 한 덩어리(내부 이음새 없음), 팀색 표시는 바깥 면에만, **HP바는 다친 셀에만**(줄지은 바 제거). 비정형(siege 28×56)·고립 셀·이미지 없음은 기존 경로 유지. 실측: 가로 4칸 벽이 소파 하나로 렌더 |
| A2 | 수풀 연녹 밑판 제거 | `rgba(46,150,86,0.30)` 판 삭제 — 타일 실루엣만 |
| A3 | 격자선 플레이 중 제거 | 에디터에서만 유지(drawEditorGrid) |
| A4 | HUD 스킬명 텍스트 제거 | 아이콘 독(링·글리프·키캡)만 |
| F19 | 바닥 어둡기 42%→22% | 테마 아트 살아남(동시 적용) |

### E16+E17 · 계정 성장 + 방치 방지 — 완료 (라이브 실측)
- **경험치**: 승 40 / 무 25 / 패 15 + 킬당 3 + 보스 10, `expNeed = 80+(Lv-1)×40` 레벨업(토스트). 죽은 필드였던 level/exp가 실제 성장으로 연결 — 로비 Lv 표시·계정 저장은 기존 배선 재사용.
- **방치 방지**: 기여(딜·킬·보스) 0이면 골드·경험치 **30%** + 결과 화면에 사유 표시("⚠ 이번 판 기여가 없어…"). 실측: 봇 캐리 승리 시 +30G·경험치+12로 감액 확인.
- 결과 화면: "획득 +NG · 경험치 +N (보유 · Lv.N)" 표기.

### F18 · 밸런스 실측 반영 (시작값 — B콘솔로 현장 조정 가능)
쿠션벽 120→**60**(시고니 7발 '부술 수 있는 문') · 수정탑 450→**650**(3인 집중 ~10초 붕괴 완화) · 별모으기 60→**90초**(역전 여지). 밸런스 콘솔 기본값 자동 추종.

### D11 · 운영자 맵 관리 — 완료 (하니스 검증)
- 교사 승인 화면(G)이 **대기+승인 두 목록**을 관리: 승인맵에서 **X=삭제**(맵 목록에서 즉시 내려감), **D=비활성/활성 토글**(기록 보존).
- `applyApprovedMaps`가 승인 목록과 MAPS를 **완전 동기화**(삭제·비활성 맵 자동 제거, editorMode 표식 사용 — 내장 맵 안전). 선택 맵이 사라지면 훈련장으로 자동 복귀.

### C8+C9 · 에디터 개선 — 완료
- **초안 자동저장**: 매 배치마다 저장, Esc로 나가도 다시 열면 복원(제출 시 정리). N=새로 만들기.
- **undo(U)**: 스냅샷 40개.
- **마우스 배치**: 좌클릭/드래그=배치, 우클릭/드래그=지우기, **팔레트 클릭 선택**, 오른쪽(미러)을 눌러도 원본 칸 편집. 지오메트리를 렌더와 공용(edGridGeom).

### E14 · 사운드 시스템 — 구조 완료 (실사운드 = 에셋 대기)
- WebAudio 모듈: `assets/sfx/<이름>.mp3`(fire·hit·kill·special·ult·win·lose·buff·reload)를 넣으면 **자동 로드·재생**, 없으면 완전 무음 no-op. 첫 입력에서 초기화(자동재생 정책).
- 훅 배선: 발사·타격·성장버프·승패. 설정(⚙)에 **효과음 볼륨** 행(0~100%, 프로필 저장).
- **대기**: 오디오 에셋 9종 제작/수급 필요 — 파일만 넣으면 켜짐.

## 기록/대기 항목 (구현 안 함 — 스펙 지시)

| # | 항목 | 상태 | 사유/권고 |
|---|---|---|---|
| B5 | 캠페인 재설계 | **설계 대기** | 스펙 명시 "사용자와 별도 설계 — 확정 전 대개편 금지". 제안 골격: 테마 구역 5개(풀→눈→사막→나무→어둠, 4스테이지씩) × 구역 보스 4종 + 성장버프 통합 + 일부 스테이지 거점 방어 변형. 확정해 주시면 다음 배치에서 구현 |
| C6 | 초대형 맵 크기(48×22) | 대기 | C7 카메라 선행 필요(현 ARENA 고정에서 만들어도 못 씀 — 대형맵 전례 방지) |
| C7 | 줌아웃/추적 카메라 | **대기(대형)** | 전 렌더가 고정 1280×720 좌표 전제 — 카메라 변환 도입은 단독 배치 권장(회귀 면적 큼) |
| D10 | 맵공방 클라우드화 | 대기(중형) | Firebase `starArenaOnline/editorMaps` 설계안 있음. 밸런스 학급 동기화와 함께 별도 배치 권장(두 클라이언트 실검증 필요) |
| D12 | 재접속 | 대기 | 방 코드+uid 재입장은 1v1 일부 존재 — 팀전 복귀 흐름은 D10과 같은 배치에서 |
| D13 | 공개 방 목록 | 대기 | 방 목록 노출 시 개인정보(닉네임) 정책도 함께 결정 필요 |
| E15 | 상점 재개 | **설계 대기** | 캐릭터·무기가 전원 기본 해금 체제라 팔 것이 없음 — 치장(스킨/이모지/명패) 상품 설계 필요. 골드 소비처 부재는 유지되는 구멍 |
| F20 | 화풍 통일·UI 아이콘 | **에셋 대기** | 눈꽃·모아 img2img 재작 + HP/공격/속도/궁 아이콘 4종 — 에셋 요청 목록에 추가 권장 |

## 테스트·마감
- **하니스 13종 ALL PASS** — 신규 검증: 승인맵 D비활성/재활성/X삭제→MAPS 동기화·선택맵 복귀, E16 경험치 지급, E17 방치 30% 배율, F18 탑 650·벽 60 반영.
- 브라우저: 에디터 맵(쿠션벽 줄)에서 벽 병합 렌더·격자 제거·수풀 정리·결과화면 경험치/방치 감액 실측. **JS 콘솔 에러 0**.
- 바이트 동일 · main push · Pages 라이브 확인(커밋은 PATCHNOTES 참조).
