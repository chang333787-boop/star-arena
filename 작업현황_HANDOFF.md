# 작업현황 · 크로스컴퓨터 핸드오프

> 다른 컴퓨터에서 이어받기: `git pull` → 이 파일부터. 세션 끝날 때 갱신 후 commit+push.

## 최신 상태 (2026-07-05, 커밋 721a05c 기준)

### 🔴 3v3 온라인 전송방식 재설계 (2026-07-07, 진행 중 — 현장 테스트 대기)
- **진단 확정:** 3v3 순간이동·렉의 근본 원인은 넷코드 로직이 아니라 **전송 경로**. 현 구조는 Firebase RTDB(asia-southeast1=싱가포르)를 15Hz 게임 중계로 사용 → 같은 교실 와이파이라도 입력·상태가 매번 한국↔싱가포르 왕복(150~300ms). v1.50/v1.51 넷코드 수정으로는 이 구조적 왕복을 못 없앰.
- **방향 결정(리서치 4갈래 wf_3cb8405b 근거):** 상용 파티게임 방식 = **WebRTC DataChannel P2P**(같은 와이파이 기기끼리 직접 연결, 왕복 5~20ms). 도입 형태는 순수 P2P가 아니라 **"WebRTC 먼저 + 실패 시 현행 RTDB 자동 폴백" 하이브리드**(TURN 없음). 시그널링만 기존 Firebase 재활용. 서버 0·교사 운영 0·단일 index.html 유지. 격리 교실도 최악의 경우 지금과 동일(퇴행 0).
  - 대안 기각: 관리형 서비스(Ably 등)=현행 대비 이득 미미(단 WS차단이 원인이면 Ably가 유일 개선책) / 자체 WS서버=교사 운영 불가+생WS는 폴백 없어 방화벽에 더 나빠짐.
- **게이트(핵심):** WebRTC 본작업(1~2주) 전에 **`nettest.html`로 실제 교실 와이파이에서 P2P 직접 연결 여부부터 실측**. 위험변수 = 학교 와이파이 클라이언트(AP) 격리 — 켜져 있으면 P2P 통째 실패, 사전 예측 불가.
  - 배포: https://chang333787-boop.github.io/star-arena/nettest.html (커밋 e7117e0). 독립 파일, index.html 무관.
  - 사용: **서로 다른 기기 2대**로 그 교실 와이파이에서 열기 → 한쪽 방만들기(4자리)·다른쪽 참가. (같은 기기 2탭은 항상 성공이라 무의미.)
  - 판정: 🟢 직접 LAN/STUN 연결됨 = 본작업 진행 / 🔴 15초 내 실패 = 격리 의심, WebRTC 넣어도 이 교실선 폴백만 씀.
  - 측정: WebRTC 왕복 vs Firebase 왕복 A/B 나란히 표시(로컬 2탭 검증치 1ms vs 160ms).
- **다음 단계:** 교사가 교실 실측 → 🟢면 하이브리드 구현 착수(writeHostState/writeInput/onlineState/onlineInput 4개 전송지점을 DataChannel로 스위치+RTDB 폴백 유지), 🔴면 Ably 폴백 검토.

### 지금 진행 중
- (없음) — **index.html 소유권 해제.** 최신: v1.54(전체 유기성 감사·21건 수정).
- **작업 원칙(2026-07-07~ 확정):** 무언가 반영할 때 **연결된 짝(온라인↔오프라인 · 에디터 팔레트→컴파일→검증→렌더 · 세이브 4곳 동승 · 터치↔키보드)을 함께 조사·반영**한다. 이 코드베이스는 로컬/온라인 규칙 엔진이 별개(updateAI↔tBotUpdate, setupRuleState↔tSetupRule, createFighters↔tMakeFighter)이고 세이브가 rpgDefaultSave/PLAYER_PROFILE_DEFAULT 화이트리스트라, 한쪽만 고치면 반드시 반쪽 갭이 남는다.
- **최근 대작업 이력:** v1.46~49 RPG 상용감 · v1.50~51 온라인 렉/순간이동 구조 수정(inputs 분리·페어 보간·탭숨김 Worker 티커·재접속 복귀) · v1.52 터치 메뉴+에디터 미리하기+WYSIWYG · v1.53 모드별 에디터(술래 개인스폰·별 랜덤·보조탑)+봇 벽슬라이드 · v1.54 유기성 감사.
- **남은 확인(실기기):** 교실 3v3 체감(게스트 우상단 🟢🟡🔴 배지 — 🔴 자주면 학교 방화벽 WebSocket 차단 의심). 보류: 성장 오브 온라인 tdm(maxHp 패킷 동기 필요 — 현재 설계상 로컬 전용).
- ✅ **`RPG_상용감개편_spec.md` 4배치 전부 구현·배포 완료(2026-07-06, Fable)**: v1.46 EXPLORE-1(추적 카메라·전체지도·보물상자 10) → v1.47 POLISH-1(밀도 26엔트리·playSec 리스폰·이름표·주스 6종·별샘 워프망·터치 3종) → v1.48 POLISH-2(하루 정산·해질녘/먼별·일일 의뢰판·온보딩·농사 2단계·정예·경제 싱크·학생 스폰 슬롯) → v1.49 POLISH-3(기부·보스 재도전·야영 취침·Esc 3버튼). 세이브 v1→2 자동 마이그레이션(playSec·resp·pity·today·daily). 전 하니스 PASS·미러 동일. 상세: PATCHNOTES v1.46~49.
- ✅ 온라인 P0(동기화 대기)는 v1.45로 완료(옆세션). 잔여 P1 렉 개선은 `온라인긴급점검_spec.md` 하단 백로그.
- **이미 반영(커밋됨)**: 로비 3층 개편(88ed94d) · BIG-BATCH-2 전체(v1.38~v1.42) · RPG 챕터1(v1.44) · 온라인 P0(v1.45) · RPG 상용감 개편(v1.46~49).

### RPG 다음 할 일
- 스펙 보류분(선택 항목): 치장(스프라이트 틴트 파이프라인 검증 후)·트리클 리스폰·Lv2 돌팔매.
- 밸런스 관찰: 밀도 증설 후 진행 곡선(두 필드 1회전 ≈ Lv7)·일일 의뢰 보상(60~120G/일)·거름 경제. 전 상수 B콘솔 "별빛 마을"에서 실시간 튜닝 가능.

### RPG-5 / 챕터 2+
- P2 학급 개방: `classes/{classCode}/flags/rpgOpen` 플래그 + **database.rules.json 확장·firebase deploy**(초안: `wiki/database.rules.초안.md`) + 로비 행 + **로비 터치화(하드 선행)**.
- 학생 콘텐츠 클라우드 미러(T2 `starArce/rpgContent`) · 도감 8탭 · 게시판 학생 퀘스트 · 요리 · 2차 전직.
- 챕터 2 지리 = 기존 캠페인 4구역 재활용. 열린 여백 7개가 학생 위키 확장 씨앗.
- 학생 콘텐츠 반영 절차: 시트 → JSON → 교사 K키 승인(즉시) 또는 T3 `wiki/rpg_db.json` 병합 후 `python3 wiki/tools/inject_db.py` → 미러 동기 → 커밋.

### 오늘 완료 (아트 — 파일 커밋+푸시됨)
- 보스 4종 치비 통일 / 지형 14장(바닥6·함정3·타일셋2·존5) / 프롭 4종 footprint 재작(수정·상자·바위·문)
- ⚠️ **에셋 파일은 있으나 대부분 게임 미배선** — 야간 spec Phase1이 배선 담당.

### 문서 지도
- **`RPG모드_PRD.md`** v1.0.1 / **`RPG_결과_20260706.md`**(구현 결과) — 교사용 RPG 모드 챕터1 **구현 완료**. 코드: index.html `const RPG_CONST`~`rpgDrawRpgReviewScreen`, STATE.RPG/RPG_REVIEW. 데이터: `wiki/`(rpg_db·rpg_maps·대사·변환표·시트양식·tools). 하니스: `harness_rpg.js`.
- **`야간_통합_spec.md`** ← Fable 현재 작업 지시 (최상위)
- `게임플레이_통합_spec.md` — 전투재설계+봇+모드+로비마감 (야간 spec의 근거)
- `전투밸런스_재설계.md` · `모드_규칙_설계.md` — 밸런스·모드 의도
- `지형타일_카탈로그.md` — 지형 5층·바닥56배수·프롭footprint 주의
- `설계_footprint_모델.md` — footprint 배선 설계
- `맵에디터_요소확정.md` — 에디터 팔레트·맵데이터 형식
- `에셋적용_감사.html` / `asset_qc.html` — 에셋 실물 확인 툴

### 확정 설계 (잊지 말 것)
- 모드: 섬멸전 · 수정부수기(공격포탑) · **점령전**(구 핫존) · 별모으기. 색칠=시즌2.
- 전투: 느린 공속→처치버프 성장 / 캐릭별 공속 / 별골렘=탱커 / 눈꽃=방해(정면공격 추가 필요) / 8방향·캐릭통일패스 보류.
- 캐릭터는 애들 디자인 — 바꿔도 디자인 보존, 화풍만 통일.
- **바닥 렌더=56 배수**(현재 256, 격자 안 맞음) · 프롭 footprint 정렬.
- 맵에디터: 모드선택→모드별 요소→점령전 좌우대칭→큰맵(줌아웃)→교사 승인 적용.

### 제약
- index.html↔star_arena.html 바이트 동일 · harness 전역 심볼 유지 · 한 세션만 index.html 편집.

## 다음 할 일
1. Fable 야간작업 결과 확인 (아침) → 판정
2. 남은 아트: UI 아이콘 4개(옵션) · 상자(block_01) 진짜 상자화(옵션)
3. 배선 검증 후 우리 코드 트랙(footprint·y-sort 등) 재개

## 2026-07-05 BIG-BATCH-2 완료 (v1.38~v1.42, Fable)
- P0 플랫포머 카메라 멀미 수정(lookahead 0.3s 유지+240px/s lerp) → P1 모드 정밀분석+보충(술래 4캐릭 모드도구·칠하기 피날레/대형도색궁·거점 이동·벽 재건·캐리어 표식·킬스트릭·**에디터 저장슬롯 5칸(S키)**) → P1.5 별빛 점프 원정 20스테이지+파워업 4종(⭐총/🌟무적/🪶깃털/🧲자석)+얼음/부서지는 블록, **전판 실물리 봇 클리어 검증(harness_pfcamp)** → P2 전 모드 온라인(3대3 방 규칙 선택 meta.rule, host 권위+RLE 페인트 동기화) → P3 맵공방 클라우드(starArenaOnline/editorMaps/학급코드, A제출→B승인→전체반영) → P4 줌아웃 카메라(22×10 회귀 0, 36×16/48×22 실플레이, 48×22 캐릭 20.2px).
- 하니스 19종 ALL PASS(신설: pfcamp·online_rules·cloud_maps·camera). 결과: `대규모업데이트2_결과_20260705.md` · `모드정밀분석_결과.md`.
- 주의: 페인트 온라인=그리드 전체 RLE(델타 아님) · 1대1=섬멸전 고정 · 에디터 배치 거점=이동 없음 · 저장슬롯은 로컬 소장(클라우드 비대상).

## 2026-07-06 온라인긴급 P0 완료 (v1.45, 커밋 1a8538c — 이 세션이 index.html 소유)
- "동기화 대기 중…" 무한 대기 = 봇 ammo undefined → RTDB set() 전체 거부 → 무음 실패. 패킷 경계 가드(am:-1=무한탄) + 전송기 딥 새니타이즈 + 무음 금지(console.warn)로 수정.
- mock DB 실서버식 엄격 검증 이식(harness_online/online_rules/online_packets) — 봇 충원 방·PVE 케이스 고정. 실기기(라이브 2클라): 게스트 85ms 동기화·경고 0. 배포 완료.
- 남은 것: spec §4 P1 렉 개선(최대 요인 = players 루트 구독의 입력 브로드캐스트 중복 → inputs/$slot 분리 + host만 구독) — 별도 커밋 권장.
