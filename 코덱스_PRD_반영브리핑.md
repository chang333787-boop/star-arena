# PRD 반영 브리핑 — 2026-07-04 세션 결정사항

> **용도**: 직장 컴퓨터 Codex에게 이 파일을 주고 **기존 PRD를 "업데이트/보강"**하게 하기 위함.
> **금지**: PRD를 처음부터 재설계하지 말 것. 기존 결정 보존 + 아래 내용만 반영.
> **직장에서 지시할 말**: "기존 PRD + `타일에셋_규격서_v0.1.md` + `감사리포트_phase0.md` + 이 브리핑을 읽고, 아래 결정사항을 PRD에 **추가·보강**해줘. 기존 내용은 지우지 말고."

---

## 1. 에셋 규격 v0.1 확정 (출처: `타일에셋_규격서_v0.1.md`)
- 시점: **3/4 오블리크 + y-sort 렌더링**
- 톤 기준: **캐릭터 톤 기준**으로 몬스터·배경 톤다운 (캐릭터가 제일 튐)
- 격자: **56px** (아레나 22×10)
- 타일(수풀·벽·바닥): **9-슬라이스 타일셋**으로 재구성, seamless 경계
- 그림자: 에셋에 굽지 않고 **엔진이 발밑 공통 타원 그림자**를 그림

## 2. Phase 0 현황 감사 완료 (출처: `감사리포트_phase0.md`, 코드와 대조 검증됨)
확인된 엔진 격차:
- **y-sort 없음** — fighters를 Y좌표로 정렬 안 함 (고정 순서 렌더)
- **fighter 공통 그림자 없음** — 구조물엔 있으나 캐릭터·몬스터엔 엔진 그림자 없음
- **ASSET_MANIFEST 선언만·미사용** — 배선 방식 확정 필요
- **footprint 데이터 모델 없음** — mapData에 px 단위 w/h만, "칸 단위" 없음 → 맵 에디터 배치 판정 불가
- **수풀·벽이 낱개 이미지** — 타일셋 아님(bush_01 하나), 오토타일 로직 없음

## 3. 작업 순서(페이징) 결정
**지금(아트 무관, 선행 가능):**
1. QA/QC 시각화 페이지 (에셋 육안 검수 도구)
2. footprint 데이터 모델 도입
3. y-sort 렌더링

**새 아트 도착 후로 연기:**
- fighter 공통 타원 그림자 켜기 (⚠️ 몬스터 재작 전에 켜면 **이중그림자** — 몬스터가 자체 그림자 박혀있을 수 있음)
- 오토타일 로직 (타일셋 필요)
- 슬라이서 v7 (타일셋 추출 기능)

## 4. 역할 분담 확정
- **그림 생성 → Codex** (`imagegen` 스킬로 로컬 규격서 읽고 생성 후 repo에 직접 저장 가능 — 업/다운로드 불필요, 검증 완료)
- **코드(슬라이싱·QC·엔진·맵에디터) → Claude Code**
- **아트 디렉션 → 사람 + `타일에셋_규격서`** (일관성이 진짜 난제, 어떤 AI툴이든 어려움)

## 5. 편집 제약 (반드시 PRD/작업 규칙에 명시)
- `index.html`과 `star_arena.html`은 **바이트 동일 유지** (해시 일치 확인됨). 한쪽 고치면 반드시 다른 쪽도 동일 반영.
- 시각화/QC 도구는 **독립 파일**(예: `asset_qc.html`)로 만들어 두 게임 파일을 건드리지 않게.

## 6. 크로스컴퓨터 핸드오프
- 단일 진입점: `작업현황_HANDOFF.md`. 세션 끝날 때 갱신 후 commit+push. 다른 컴퓨터는 pull 후 그 파일부터 읽음.
